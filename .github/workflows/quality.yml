name: ML Portfolio Quality

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate-repository:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Validate notebook and portfolio artifacts
        run: python scripts/validate_project.py
